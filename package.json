{
  "name": "nuxtbot",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "deploy": "NITRO_PRESET=lagon bun nuxt build && lagon deploy --prod"
  },
  "devDependencies": {
    "bun-types": "^0.4.0"
  },
  "dependencies": {
    "nuxt": "^3.5.2",
    "ofetch": "^1.0.0"
  }
}
